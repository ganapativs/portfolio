import styled from 'styled-components';

const SwitcherButton = styled.div`
  position: relative;
  padding: 10px;
  margin-left: 2rem;
  border-radius: 50%;
  width: 44px;
  height: 44px;
  cursor: pointer;
`;

export default SwitcherButton;
